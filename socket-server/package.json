{
	"author": "",
	"dependencies": {
		"@prisma/client": "^4.5.0"
	},
	"description": "",
	"devDependencies": {
		"@types/body-parser": "^1.19.2",
		"@types/express": "^4.17.14",
		"axios": "^1.1.3",
		"body-parser": "^1.20.1",
		"cors": "^2.8.5",
		"date-fns": "^2.29.3",
		"dotenv": "^16.0.3",
		"esbuild": "^0.15.12",
		"express": "^4.18.2",
		"express-rate-limit": "^6.6.0",
		"lodash": "^4.17.21",
		"node-fetch": "^3.2.10",
		"nodemon": "^2.0.20",
		"prisma": "^4.5.0",
		"random-word-slugs": "^0.1.6",
		"socket.io": "^4.5.3",
		"typescript": "^4.8.4"
	},
	"license": "ISC",
	"main": "index.js",
	"name": "socket-server",
	"scripts": {
		"bundle": "npm i && npx prisma generate && npx esbuild ./index.ts --bundle --outfile=index.js --platform=node",
		"dev": "npm run bundle --watch & nodemon index.js",
		"start": "npx prisma migrate deploy && node index.js",
		"tsc": "tsc",
		"deploy": "npm prune --omit=dev && fly deploy --remote-only"
	},
	"version": "1.0.0"
}
